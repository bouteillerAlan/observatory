import React from 'react';
import history from '../server/history';

const Nav: React.FunctionComponent = () => {
  return (
    <div style={{backgroundColor: 'grey', height: '50px', width: '100%'}}>
    </div>
  )
}

export default Nav;
